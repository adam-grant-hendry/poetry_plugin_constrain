[tool.poetry]
name = "test"
version = "0.1.0"
description = ""
authors = ["<author@test.com>"]

[tool.poetry.dependencies]
python = ">=3.8, <3.11"
poetry-plugin-constrain = ">=0.1.0"

[tool.poetry-plugin-constrain]
enable-post-hooks = false
